export type CatchCopyStateType = {
    shopUid: string,
    catchCopy: string,
    catchCopyColorBase: number,
    catchCopyColorMoji: number,
    catchCopyColorSub: number,
    pattern: string,
    editMode: boolean
}